#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../bump-cli/bin/bump" "$@"
  ret=$?
else 
  node  "$basedir/../bump-cli/bin/bump" "$@"
  ret=$?
fi
exit $ret
